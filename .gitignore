node_modules
package-lock.json
yarn.lock

.DS_Store
.cache
.env
.env.test
.vercel
.output
/build/
/server/build
/public/build
/dist/

# Sentry Config File
.env.sentry-build-plugin
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

/src/generated/prisma

# clerk configuration (can include secrets)
/.clerk/

# Vitest coverage output
coverage/
.env*

# Browser-driver artifacts (screenshots, saved auth state)
/.screenshots/
/.auth/

# Editor/tool backups. Windows Perl's `-i` writes a .bak by default (GNU
# Perl's does not), so in-place edits during mutation testing silently
# littered these into the repo once.
*.bak
*.orig
